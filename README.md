🚀 Level 1 — Your First Mission

You just joined a company.

Your team gives you a project with this structure:

calculator/
    app.js

Current content of app.js:

console.log("Calculator");

You add this new function:

function add(a, b) {
    return a + b;
}

Now your task is to:

Check the repository status.
Stage the modified file.

Commit it with the message:

Add addition function
View the commit history.
